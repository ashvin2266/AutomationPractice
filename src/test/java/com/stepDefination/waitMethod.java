package com.stepDefination;

public class waitMethod extends DriverClass {
//    WebDriverWait wait= new WebDriverWait(driver,60);


//    Thread.sleep(6000) that is java command

//    implicit wait

//    driver.manage().timeouts().implicitlyWait(10, TimeUnit.SECONDS); that is selenium command


//    Explicit Wait

// WebDriverWait wait= new WebDriverWait(driver,60);
//    this should be top of the class
// wait.until(ExpectedConditions.visibilityOf((WebElement)By.name("id_gender"))).click();
//// Fluent wait

//     Wait wait = new FluentWait(driver)
//        .withTimeout(10,TimeUnit.SECONDS);
//    Wait<WebDriver> wait = new FluentWait<WebDriver>(driver)
//            .withTimeout(60,TimeUnit.SECONDS)
//            .pollingEvery(5,TimeUnit.SECONDS) //every 5 second find element
//            .ignoring(NoSuchElementException.class);  // when not element find give error
//































}
